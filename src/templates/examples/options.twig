{% for variant in craft.variantManager.getVariantsByOptions(
	7637,
	[
		["What tire pressure are your tires running at?", "65PSI"],
		["What tire pressure are your tires running at?", "60PSI"]
	],
) %}

	{{ variant.id }}

{% endfor %}
